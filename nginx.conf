server {
    listen 443 ssl;
    server_name devops.melodymocktail.com;

    ssl_certificate /etc/nginx/ssl/your_domain.crt;
    ssl_certificate_key /etc/nginx/ssl/your_domain.key;
    
    # Optional: Include the certificate chain
    # ssl_trusted_certificate /etc/nginx/ssl/your_domain.ca;

    # Other SSL configurations (e.g., protocols, ciphers, etc.) go here

    location / {
        # Your regular server configuration
    }
}
